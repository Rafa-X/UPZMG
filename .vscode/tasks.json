
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "command": "dotnet",
      "type": "process",
      "args": ["build", "-c", "Debug", "${workspaceFolder}/UPZMG.sln"],
      "problemMatcher": "$msCompile"
    }
  ]
}
// Makes sure both projects compile before starting.